1. Put `EventStoryLine` dataset in the current directory.
2. Run `read_document.py' to build all intra-sentence examples.
3. Run `preprecess_*` to generate BERT-based examples.
4. Run `train_mix` to train the model.